# ============================================================
# AuthDoc Frontend — Environment Variables
# ============================================================
# Copy this file to `.env` and fill in the values.
# Never commit .env to version control.
# ============================================================

# Backend API base URL (Node.js server)
VITE_API_BASE_URL=http://localhost:3000/api
